// import 'package:add_2_calendar/add_2_calendar.dart';
import 'package:check_in_domain/check_in_domain.dart';

void presentCalendarEvent(ActivityManagerForm? activity, ReservationTimeFeeSlotItem slot, String? listingTitle, String activityTitle) {
  // final Event event = Event(
  //   title: activity?.profileService.activityBackground.activityTitle.value.fold((l) => (listingTitle != null) ? '$activityTitle at $listingTitle' : '$activityTitle Activity', (r) => r) ?? 'Reservation',
  //   description: activity?.profileService.activityBackground.activityDescription1.value.fold((l) => '', (r) => r) ?? '',
  //   allDay: isFullDaySlot(slot),
  //   startDate: slot.slotRange.start,
  //   endDate: slot.slotRange.end,
  //   iosParams: IOSParams(
  //     reminder: Duration(minutes: 30), // on iOS, you can set alarm notification after your event.
  //     // url: 'https://www.example.com', // on iOS, you can set url to your event.
  //   ),
  //   // androidParams: calendar.AndroidParams(
  //   //   emailInvites: [], // on Android, you can add invite emails to your event.
  //   // ),
  // );
  // Add2Calendar.addEvent2Cal(event);

}